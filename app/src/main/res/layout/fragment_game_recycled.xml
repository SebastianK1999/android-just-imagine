<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/card_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center_vertical"
        android:paddingTop="100dp"
        android:clipToPadding="false"
        />
    <TextView
        android:id="@+id/recycled_warning"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="50dp"
        android:layout_gravity="center"
        android:hint="game.recycled.waring"
        android:textSize="24sp"
        android:visibility="invisible"/>

    <RelativeLayout
        android:id="@+id/recycled_top"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/Bubble"
        android:padding="10dp"
        android:orientation="vertical">
        <Button
            android:id="@+id/game_draw"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bubble"
            android:hint="game.recycled.draw"
            android:text="@string/game_recycled_draw"
            android:layout_alignParentRight="true"
            android:textSize="18sp"/>
    </RelativeLayout>
</RelativeLayout>